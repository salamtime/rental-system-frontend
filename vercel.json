{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    }
  ],
  "routes": [
    { "src": "/(.*\\.(js|css|map|png|jpg|svg|ico|json))", "dest": "/$1" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}
